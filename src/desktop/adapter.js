// Stable desktop entry point.  The implementation stays separate from the
// transport so the old characterization fixtures and the loopback bridge use
// exactly the same command adapter.
export { DesktopAdapter } from "../desktop-adapter.js";
